import ScreenPlaceholder from '../components/ScreenPlaceholder'

export default function HomeScreen() {
  return (
    <ScreenPlaceholder title="Home" phase="Coming in Phase 4">
      Your suggested session, streak, and this week's minutes will live here. For
      now, tap around the bottom nav to see the skeleton hold together.
    </ScreenPlaceholder>
  )
}
