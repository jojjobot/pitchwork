import ScreenPlaceholder from '../components/ScreenPlaceholder'

export default function SettingsScreen() {
  return (
    <ScreenPlaceholder title="Settings" phase="Coming in Phase 4">
      Your weekly goal, sound and vibration, default equipment, and tools to export
      or import all your data.
    </ScreenPlaceholder>
  )
}
