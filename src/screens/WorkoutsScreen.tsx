import ScreenPlaceholder from '../components/ScreenPlaceholder'

export default function WorkoutsScreen() {
  return (
    <ScreenPlaceholder title="Workouts" phase="Coming in Phase 2">
      Ready-made sessions you can filter by time, goal, difficulty, and the kit
      you actually have — even if that's just a ball and a wall.
    </ScreenPlaceholder>
  )
}
