import ScreenPlaceholder from '../components/ScreenPlaceholder'

export default function LibraryScreen() {
  return (
    <ScreenPlaceholder title="Exercise library" phase="Coming in Phase 1">
      Every drill, sorted by category, with step-by-step instructions and coaching
      cues so you can do each one right without a video.
    </ScreenPlaceholder>
  )
}
