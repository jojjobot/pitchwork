import ScreenPlaceholder from '../components/ScreenPlaceholder'

export default function BuilderScreen() {
  return (
    <ScreenPlaceholder title="Workout builder" phase="Coming in Phase 3">
      Build your own session: pick drills, set duration or reps, reorder them, and
      watch the estimated time and category mix update as you go.
    </ScreenPlaceholder>
  )
}
